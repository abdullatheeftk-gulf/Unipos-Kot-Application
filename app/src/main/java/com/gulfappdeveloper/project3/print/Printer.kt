package com.gulfappdeveloper.project3.print

/*class Printer(
    private val address: String,
    private val port: Int,
    private val timeOut: Int

) {

    val pr = EscPosPrinter(
        TcpConnection(
            address,
            port,
            timeOut
        ),
        280,
        66f,
        44
    )

    suspend fun printKot(
        text: String
    ) {
        try {
            EscPosPrinter(
                TcpConnection(
                    this.address,
                    this.port,
                    this.timeOut
                ),
                280,
                66f,
                44
            ).printFormattedText(
                text
            )
        } catch (e: Exception) {
            e.printStackTrace()
        }

    }
}*/
